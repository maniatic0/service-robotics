#pragma once
#ifndef CONTROL_H
#define CONTROL_H

extern bool on_intersection;
extern bool entering_intersection;
extern bool exiting_intersection;
extern bool top_corner; // If we are on top of a top_corner afer hitting the right right sensor
extern bool white_corner; // for cross intersections
extern bool black_corner; // for cross intersections
extern bool second_white_corner; // for cross intersections

#endif